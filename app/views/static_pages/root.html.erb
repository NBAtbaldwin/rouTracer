<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>
  window.phoneImage = "<%= asset_path("phone.png")%>"
  window.loginBiker = "<%= asset_path("tourdefrance.png")%>"
</script>
<div id='root'></div>
